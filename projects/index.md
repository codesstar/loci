---
description: Light index of serious projects — the brain holds only this; full memory lives in each project's own repo.
updated: 2026-05-30T14:36:35+10:00
---

# Projects

<!--
This is a LIGHT INDEX, not a database. The brain aggregates, it does not own:
each serious project's full memory lives in ITS OWN repo (`.loci/memory.md` +
`.loci/decisions/`). This file holds one short entry per project so the AI knows
the project exists and where to find it.

The AI adds an entry here when it connects a serious project (offered once, on the
user's yes — there is no command to learn). Read the project's repo for detail.

Siblings in this `projects/` directory: `index.md` (this file — serious projects) and
`side.md` (project embryos not serious enough for their own repo yet). All project-related
memory lives under `projects/`; `me/` is only about who the user is.

Format:

## project-name   <!-- status: active | dormant | archived -->
One-line essence of what this project is. repo: <path>. memory: <repo>/.loci/memory.md
Key decisions: [[brain-decision-slug]]   (only if a decision was promoted to brain level)
updated: <ISO8601>
-->
